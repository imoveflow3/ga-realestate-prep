#!/usr/bin/env bash
# Double-click this file in Finder to start studying.
# It opens your browser automatically. Keep this window open; close it to stop.
cd "$(dirname "$0")"
GAPREP_OPEN=1 exec python3 server.py
